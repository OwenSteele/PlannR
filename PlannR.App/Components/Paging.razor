@if (TotalPages > 0)
{
    <div class="float-left paging mt-3">
        <button type="button" class="@((PageIndex == 1) ? "paging-btn-disabled" : "paging-btn")" @onclick="@(() => OnClick.InvokeAsync(1))">
            <i class="fa fa-angle-double-left" aria-hidden="true"></i>
        </button>
        <button type="button" class="@(HasPreviousPage ? "paging-btn" : "paging-btn-disabled")" @onclick="@(() => OnClick.InvokeAsync(PageIndex - 1))">
            <i class="fa fa-angle-left" aria-hidden="true"></i>
        </button>
        <span>@PageIndex</span>
        <button type="button" class="@(HasNextPage ? "paging-btn" : "paging-btn-disabled")" @onclick="@(() => OnClick.InvokeAsync(PageIndex + 1))">
            <i class="fa fa-angle-right" aria-hidden="true"></i>
        </button>
        <button type="button" class="@((PageIndex == TotalPages) ? "paging-btn-disabled" : "paging-btn")" @onclick="@(() => OnClick.InvokeAsync(TotalPages))">
            <i class="fa fa-angle-double-right" aria-hidden="true"></i>
        </button>
    </div>
}